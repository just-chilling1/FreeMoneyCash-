# Create Page Visual Refresh

**Date:** 2026-09-17  
**Status:** Approved  
**Scope:** Visual polish only — same 2-step flow, no logic/API changes

## Goal
Align `/create` with the dashboard visual language (gold/olive, clearer hierarchy, less glow noise).

## Changes
1. **Hero** — Eyebrow `Create`, bold headline, short support line (dashboard pattern).
2. **Step indicator** — Slimmer progress rail; smaller step markers; tighter padding.
3. **Niche cards** — Gold-bordered tiles; hover border lift without scale; no default glow/scan clutter.
4. **Generate step** — Same content; cleaner panel hierarchy; primary CTA emphasis; tighter progress/success states.

## Out of scope
Alerts→toasts, validation rewrites, new steps, major copy changes.
